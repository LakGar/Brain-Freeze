import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AddRelax = () => {
  return (
    <View>
      <Text>AddRelax</Text>
    </View>
  );
};

export default AddRelax;

const styles = StyleSheet.create({});
